# 1번 김기민
import seaborn as sns
iris=sns.load_dataset('iris')
x = iris['petal_length']
y = iris['sepal_length']
x=x.to_numpy()
print(x)
print(y) # 1번 시연 (김기민) + 2번 시연 (김기민)
print(iris) # 2번 시연 2차

# 2번 어동준
from sklearn.model_selection import train_test_split
train_x, test_x, train_y, test_y = train_test_split(x, y, random_state=42589)
train_x = train_x.reshape(-1, 1)
test_x = test_x.reshape(-1, 1)
test_y= test_y.reshape(-1, 1) # 1번 시연 (이세민)
print(train_x) # 1번 시연 (어동준)
print(test_x) # 1번 시연 (임재백) + 2번 시연 (임재백)
print(train_y) # 시연의 시연의 시연을 거쳐보자

# 3번 임재백
from sklearn.linear_model import LinearRegression
reg_iris=LinearRegression()
reg_iris.fit(train_x, train_y)
print(reg_iris.coef_, reg_iris.intercept_)

# 4번 이세민
# 2번 시연 (임재백)
# 김기민 branch
print(x)
# 어동준 branch
# 2번 시연
